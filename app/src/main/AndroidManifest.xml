<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vikinzi.vikingsyambdventure">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.LoadingScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SignupActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.ResetPasswordActivity" />
        <activity android:name=".activities.NewGameActivity">
            <intent-filter>
                <action android:name="android.intent.action.NEWGAME" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.PropertiesActivity">
            <intent-filter>
                <action android:name="android.intent.action.PROPERTIES" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.GameRulesActivity" />
        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.MiniYamb" />
        <activity android:name=".activities.MiniYambOpponent" />
        <activity android:name=".activities.MaxiYamb" />
        <activity android:name=".activities.MaxiYambOpponent" />
        <activity android:name=".activities.AccountActivity" />
        <activity android:name=".activities.FindMaxiActivity" />
        <activity android:name=".activities.FindMiniActivity" />
        <activity android:name=".activities.NewSinglePlayerActivity" />
        <activity android:name=".activities.MiniSingle" />
        <activity android:name=".activities.MaxiSingle"/>
    </application>

</manifest>